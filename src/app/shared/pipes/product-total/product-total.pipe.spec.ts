import { ProductTotalPipe } from './product-total.pipe';

describe('ProductTotalPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductTotalPipe();
    expect(pipe).toBeTruthy();
  });
});
